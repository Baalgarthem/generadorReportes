import 'package:flutter/material.dart';

class PantallaVerReportes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pantalla de Ver Reportes'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de ver reportes'),
      ),
    );
  }
}
